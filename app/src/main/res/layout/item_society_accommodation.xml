<?xml version="1.0" encoding="utf-8"?>
<!-- item_society_accommodation.xml
     One card per society. Tap the header to expand/collapse events. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_margin="6dp">

    <!-- Society header (tappable to expand) -->
    <TextView
        android:id="@+id/tvSocietyName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="14dp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="#8B1A4A"
        android:text="Society Name ▼"/>

    <!-- Events container (collapsed by default) -->
    <LinearLayout
        android:id="@+id/llEvents"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#FFFFFF"
        android:visibility="gone"/>

</LinearLayout>